{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "// test": "mocha --debug-brk --reporter spec --inspect server/**/*.test.js",
    "test": "mocha --reporter spec --inspect server/**/*.test.js",
    "test-watch": "nodemon -e test.js --exec \"npm test\""
  },
  "engines": {
    "node": "10.9.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "moment": "^2.22.2",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "expect": "^23.5.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.18.4"
  }
}
